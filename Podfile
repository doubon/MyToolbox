# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'MyDateTime' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MyDateTime

end

target 'MyToolbox' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MyToolbox
  pod 'IQKeyboardManagerSwift', '5.0.3'

  target 'MyToolboxTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MyToolboxUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
